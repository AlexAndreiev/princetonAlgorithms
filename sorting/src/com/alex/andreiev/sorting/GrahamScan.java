package com.alex.andreiev.sorting;

public class GrahamScan
{
    static public void DoScan(Point2D[] p){
//        Stack<Point2D> hull = new Stack<Point2D>();
//        Arrays.sort(p, Point2D.YORDER); // p[0] is now point with lowest y-coordinate
//        Arrays.sort(p, p[0].BY_POLAR_ORDER); // sort by polar angle with respect to p[0]
//
//        hull.push(p[0]); // definitely on hull
//        hull.push(p[1]);
//
//        for (int i = 2; i < p.length; i++) {
//            Point2D top = hull.pop();
//            while (Point2D.ccw(hull.peek(), top, p[i]) <= 0)
//                top = hull.pop();
//            hull.push(top);
//            hull.push(p[i]); //add p[i] to putative hull
//        }
    }
}
